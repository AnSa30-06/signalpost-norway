# virtual environments and caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# run outputs (each run writes its own directory; never committed)
out/
work/

# the rendered site is rebuilt from envelopes; the committed artifact lives in submission/
site/

# the 12 MB public universe file is downloaded, not committed
signalpost-company-universe-2025.jsonl.gz
